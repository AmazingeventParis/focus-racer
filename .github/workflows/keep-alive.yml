name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -sf "${{ secrets.RENDER_URL }}/api/auth/providers" -o /dev/null || true
